#include "holberton.h"
#include <stdio.h>
/**
 *mul - multiplies integers
 *@a: integer to be entered
 *@b: integer to be entered
 *Return: returns the multiplication result of a and b
 */

int mul(int a, int b);
{
return (a * b);
}
