#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  printf("size of char: %D\n", size of (char));
  printf("size of integer: 5d\n", size of (int));
  printf("size of float: %d\n", sizeof(float));
    return (0);
}
