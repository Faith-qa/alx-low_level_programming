#include "holberton.h"
#include <stdio.h>
/**
 *_strlen - returns the length of a string
 *@s: integer entered
 *Return: void
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; ++i)
{
}
return (i);
}
