#include "holberton.h"
/**
 *int _islower(int c); - checks for lowercase characters
 *
 * Return: (1) if lowercase and  (0) if its not
 *
 */
int _islower(int c)
{
char c;
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
return (0);
}
